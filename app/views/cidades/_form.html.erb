<%= form_for(@cidade) do |f| %>
  <% if @cidade.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cidade.errors.count, "error") %> prohibited this cidade from being saved:</h2>

      <ul>
      <% @cidade.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br>
    <%= f.text_field :nome, :class=>"form-control",:placeholder=>"Nome" %>
  </div>
  <div class="field">
    <%= f.label :estado %><br>
    <%= f.number_field :estado, :class=>"form-control",:placeholder=>"Estado" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
